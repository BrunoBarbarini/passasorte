{
  "name": "@passasorte/worker",
  "version": "0.0.0",
  "private": true,
  "description": "PassaSorte scheduled operations worker (CLAUDE.md #10/#28) - TASK-033 Outbox Worker + TASK-034 Scheduler.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@passasorte/analytics": "workspace:*",
    "@passasorte/application": "workspace:*",
    "@passasorte/config": "workspace:*",
    "@passasorte/domain": "workspace:*",
    "@passasorte/observability": "workspace:*",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  }
}
