/**
 * @passasorte/api-contract
 *
 * Shared request/response/DTO types (and, later, generated OpenAPI types)
 * for the `/api/v1` REST surface described in CLAUDE.md #16, consumed by
 * apps/api, apps/web and apps/mobile so client and server never drift.
 *
 * Deliberately empty in Phase 0 (Foundation); first contracts land
 * alongside the catalog/campaign endpoints in Phase 1.
 */
export {};
