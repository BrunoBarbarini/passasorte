/**
 * @passasorte/testkit
 *
 * Shared test utilities: factories, fixtures, deterministic game seeds and
 * synthetic identities (CLAUDE.md #40). Never seeds or references real
 * production customer data.
 *
 * Deliberately empty in Phase 0 (Foundation); factories are added as each
 * bounded context gains entities, starting Phase 1.
 */
export {};
