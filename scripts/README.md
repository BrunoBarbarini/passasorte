# scripts/

Operational and developer scripts (seeding, one-off migrations helpers,
local bootstrap). Empty in Phase 0; the first script is expected to be a
DB seed script once the schema exists (Phase 1, CLAUDE.md #40).
